import SmartSearchExplorer from './Discovery/SmartSearchExplorer';

/**
 * SmartFilter Component
 * Provides real-time dynamic places filtering, category switching,
 * and location-tied dynamic imagery with category-safe fallbacks.
 */
export default SmartSearchExplorer;
